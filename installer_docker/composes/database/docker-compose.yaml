name: local_database_for_${DB_NAME}

services:
  
  database:
    image: artecoglobalcompany/usee_database:${DOCKER_TAG:-latest}
    ports:
      - "${DB_PORT:-27017}:27017"
    volumes:
      - mongodb_data:/data/db_${DB_NAME}
    container_name: ${DB_NAME}
    restart: unless-stopped
    
volumes:
  mongodb_data:
      name: database_for_${DB_NAME}  # Nome unico per il volume
